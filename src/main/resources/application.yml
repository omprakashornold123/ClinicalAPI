spring:
  application:
    name: clinicals-api
  datasource:
    url: jdbc:mysql://localhost:3306/clinicals?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 9090